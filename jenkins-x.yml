buildPack: go-cli
pipelineConfig:
  pipelines:
    release:
      preBuild:
        steps:
        - name: fetch-tags
          sh: git fetch --tags
      promote:
        steps:
        - command: ./promote.sh
          dir: /workspace/source
          name: promote-release
